import {
    Flex,
    Box,
    Text,
    Input,
    useColorModeValue,
    Stack,
} from "@chakra-ui/react"

export default function Word() {
    return (
        <Flex>
            <Text>Word</Text>
        </Flex>
    );
}
