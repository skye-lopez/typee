import { useState, useEffect } from "react"
import {
    Flex,
    Box,
    Text,
    Input,
    useColorModeValue,
} from "@chakra-ui/react"

export default function Game() {
    return (
        <Flex bg='red'>
        </Flex>
    );
}
